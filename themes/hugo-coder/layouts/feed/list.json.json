{
    "latest-post": {
        "url": "{{ (index .Site.RegularPages 0).RelPermalink }}",
        "title": "{{ (index .Site.RegularPages 0).Title }}",
        "description": "{{ (index .Site.RegularPages 0).Description }}",
        "publish-date": "{{ (index .Site.RegularPages 0).PublishDate }}"
    }
}
